<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item"
    android:background="@android:drawable/dialog_holo_light_frame"
    android:layout_width="match_parent"
    android:layout_height="@dimen/search_item_height">

    <LinearLayout
        android:id="@+id/text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/poster"
            android:layout_width="@dimen/search_poster_dimension"
            android:layout_height="@dimen/search_poster_dimension"
            android:src="@drawable/movie_search_placeholder"
            android:padding="@dimen/smallest_padding"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="@dimen/smaller_padding"
            android:paddingBottom="@dimen/smaller_padding">

            <TextView
                android:id="@+id/title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Inception"
                android:textStyle="bold"
                android:paddingLeft="@dimen/smallest_padding"
                android:textSize="@dimen/text_size_default"
                android:singleLine="true"
                android:paddingBottom="@dimen/smallest_padding"/>

            <TextView
                android:id="@+id/genres"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Action, Adventure, Thriller"
                android:paddingLeft="@dimen/smallest_padding"
                android:singleLine="true"
                android:textSize="@dimen/text_size_small" />
        </LinearLayout>
    </LinearLayout>
</RelativeLayout>

